import { Container, Typography } from "@mui/material";

export default function Loader(){
    return(
        <Container sx={{display:"flex", justifyContent:"center", alignItems:"center"}}><Typography variant="h2">Loading...</Typography></Container>)
            
        
}